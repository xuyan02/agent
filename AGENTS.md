## Mi Code Added Memories
- User prefers Plan Tool implemented as a new plan2 system (do not modify existing infra/plan), with UUID visible to LLM and in tool arguments; top-level tasks must include report_to explicitly; add_tasks auto-computes initial status; Markdown render uses v3 indentation with id on its own line and children block.
- In the parallel tool-call design discussion, user defines had_tool_calls_ to be set true on OnToolCalls arrival and reset to false in OnRequestDone (same round).
- In this project, exceptions are forbidden (do not use try/catch or rely on exceptions).
- For this project’s upcoming poll-based async refactor: (1) Intermediate/combining futures must NOT store MessageLoop or waker; only low-level IO futures may store MessageLoop::Current() and a weak WakerHandle. (2) Use PollFuture/PollContext carrying a WakerHandle (weak) to avoid UAF; event callbacks only set flags + Wake; executor owns waker and drives polling via PostTask with coalescing. (3) For functions that return a Future (async-like), write async/await pseudocode first as comments, and define the corresponding Future implementation class locally inside the function body.
- Poll-mode refactor decision: Big-bang cutover (option A). Old Then/Catch/Subscribe Promise-based Future/Promise will be removed; upper-layer code may be temporarily non-compiling and re-enabled step-by-step. New poll-mode type is named dust::Future. MessageLoopExecutor lives in message_loop module and is a member of dust::MessageLoop.
- In poll-mode: Waker instances must inherit dust::SupportsWeakPtr<Derived> so they can be referenced via dust::WeakPtr and stored in WakerHandle safely.
- When writing async/await pseudocode comments for poll-mode Future migrations, use C++-style syntax (no non-C++ keywords like `await`; express awaits as `co_await`-like comments or explicit polling steps in C++-ish pseudocode).
- Pseudo-code rule: downstream implementation must be a line-by-line translation of the pseudo-code (await#N points map directly to state machine steps).
- Project convention (poll-mode state machines): (1) Only terminal state (kDone / kError) returns Ready directly; other states must set terminal state then fallthrough to it. (2) Any time a state advances state_ to the next await or creates/replaces a subfuture, it must immediately fallthrough (or equivalently execute) into that next state and poll the subfuture at least once in the same PollOnce tick—never assume first await will yield Pending or that a Wake will occur before first poll.
- Project conventions/lessons (poll-mode futures): Avoid started_/done_ booleans to drive control flow; implement explicit state machine with kInit/kAwaitN/kDone. Never assume first PollOnce returns Pending. Any subfuture/IO is typically armed on first PollOnce, so when you create/replace a subfuture or advance state_ you must immediately execute next state and poll it at least once in the same PollOnce tick (often via [[fallthrough]]). Non-terminal states should not return Ready directly; set terminal state and fallthrough. Symptom of violation: executor drives once, PollOnce->Pending, then no Wake/ScheduleDrive.
